type Query {
  users: [User!]!
}

type Mutation {
  createUser(username: String!, password: String!): AuthPayload!
}

type User {
  id: ID!
  username: String!
}

type AuthPayload {
  user: User!
  token: String!
}
